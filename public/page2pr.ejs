<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Select Options Form</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f7f7f7;
            margin: 0;
            padding: 0;
        }
        h1 {
            text-align: center;
        }
        form {
            max-width: 500px;
            margin: 20px auto;
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        label {
            display: block;
            margin-bottom: 10px;
            font-weight: bold;
        }
        select, input[type="submit"] {
            width: calc(100% - 22px);
            padding: 10px;
            margin-bottom: 15px;
            border: 1px solid #ccc;
            border-radius: 5px;
            box-sizing: border-box;
            font-size: 16px;
            margin-left: 22px;
        }
        button[type="submit"] {
            background-color: #4CAF50;
            color: white;
            border: none;
            cursor: pointer;
            width: 100%;
            padding: 10px;
            border-radius: 5px;
            font-size: 16px;
        }
        button[type="submit"]:hover {
            background-color: #45a049;
        }
    </style>
</head>
<body>
    <h1>Select Options Form</h1>
    <form action="/page2pr_sub" method="POST">
        <% 
            let arr = [];
            staffarr = [];
            let tarr = lecpr==='lec'?['8:00-9:00','9:00-10:00','10:00-11:00','11:00-12:00','12:50-1:50','1:50-2:50','2:50-3:50','3:50-4:50']:['8:00-10:00','10:00-12:00','1:00-3:00','3:00-5:00'];
            if(evenodd==='even') {
                if(classnm==='FY1' || classnm==='FY2' || classnm==='FY3') {
                    if(lecpr==='lec') {

                    } else {

                    }
                } else if (classnm==='SY1' || classnm==='SY2' || classnm==='SY3') {
                    if(lecpr==='lec') {

                    } else {
                        
                    }
                } else {
                    if(lecpr==='lec') {
                        arr = ['MGT','PWP','MAD','ETI','WBP','EDE'];
                    } else {
                        arr = ['PWP','MAD','WBP','EDE','CPE'];
                    }
                }
            } else {
                if(classnm==='FY1' || classnm==='FY2' || classnm==='FY3') {
                    if(lecpr==='lec') {

                    } else {

                    }
                } else if (classnm==='SY1' || classnm==='SY2' || classnm==='SY3') {
                    if(lecpr==='lec') {

                    } else {
                        
                    }
                } else {
                    if(lecpr==='lec') {
                        
                    } else {
                        
                    }
                }
            }
        %>

        <label for="time">Select Timing:</label>
        <select name="time" id="time">
            <%
                tarr.forEach((x) => {
            %>
            <option value="<%=x%>"><%=x%></option>
            <%                  
                });
            %>
        </select>
        <br>
        <label for="day">Select Day:</label>
        <select name="day" id="day">
            <%
            days = ['mon','tue','wed','thu','fri','sat'];
            days.forEach(x => {
            %>
            <option value="<%=x%>"><%=x%></option>
            <%
            });
            %>
        </select>
        <br>
        <label>Select Subjects for Batch A:</label>
        <select name="batch_a" id="batch_a">
            <%
                arr.forEach((x) => {
            %>
            <option value="<%=x%>"><%=x%></option>
            <%
                });
            %>
        </select>
        <br>
        <label>Select Subjects for Batch B:</label>
        <select name="batch_b" id="batch_b">
            <%
                arr.forEach((x) => {
            %>
            <option value="<%=x%>"><%=x%></option>
            <%
                });
            %>
        </select>
        <br>
        <label>Select Subjects for Batch C:</label>
        <select name="batch_c" id="batch_c">
            <%
                arr.forEach((x) => {
            %>
            <option value="<%=x%>"><%=x%></option>
            <%
                });
            %>
        </select>
        <br>
        <input type="hidden" name="classnm" value="<%= classnm %>">
        <button type="submit">Submit</button>
    </form>
</body>
</html>
